# Building `tmux` inside Docker

This repository contains some basic `Dockerfiles` to help me learn how to build
static copies of `tmux` inside different OS images.

It doesn't really have much use beyond that.

**Current OS Support**
- Ubuntu 22.04
- Alpine 3.19
